// Schema para produção com MySQL
// Este arquivo é uma cópia do schema.prisma mas configurado para MySQL

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Copie todo o conteúdo do schema.prisma a partir daqui
// Os models permanecem os mesmos, apenas o datasource muda

